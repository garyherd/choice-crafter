import React, { Component } from 'react';

class DecisionsFree extends Component {
    render() {
      return (
      <div>
          This will be where a free user can start trying the app. Decisions will only be available
          via local/session storage.
      </div>
    );
  }
}

export default DecisionsFree;